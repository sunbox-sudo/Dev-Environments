#!/usr/bin/env bash

# update syteam
pacman -Syu --noconfirm

# Updates app/packages
pacman -S -noconfirm

# uppdate dotfiles

cd $HOME/dev/.dotfiles
git pull origin main

stow -t $HOME .
